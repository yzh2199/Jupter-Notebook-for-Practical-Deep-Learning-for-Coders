1. 部署一个线上的应用
    1. 第一步是清洗数据
        1. 首先一个思想，模型先于清洗数据，其实是用未训练的模型去对数据进行分类，这样能快速知道那些极端错误的数据
        2. 混淆矩阵：对于结果是类别的数据有用，某行某列的意思是数据的类别本来是行结果被识别成列的个数
        3. loss的含义：那些被错误识别且模型给的概率还很高的数据和那些被正确识别但是模型给的概率比较低的数据